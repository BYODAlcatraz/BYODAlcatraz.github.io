- name: Create a file with specific content in the Documents directory
  hosts: localhost
  tasks:  - name: Create whitelist
    copy:
      dest: "/etc/squid/whitelist.conf"
      content: |
        stackoverflow.com
